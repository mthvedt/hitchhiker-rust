package com.thunderhead.core

/**
  * Created by mike on 7/25/16.
  */
trait ManifoldInfo {
//  def split(resourceLevel: Resource): IndexedSeq[ManifoldInfo]

  // Idea: Self-organizing collections of resources. Manifold starts from the ground up,
  // not from the top down, as nodes discover where they are in space.
}
